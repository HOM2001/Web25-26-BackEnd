<?php



const ROOT_DIR = "../app/";

const AJAX_ENABLED = true;   // false;

const MEDIA_PATH = './media/';

const DEFAULT_ORDER = 'recent'; // first(via id =); / last(via id ) / old ( via date ) / recent (via date)/ random

const DEFAULT_LIMIT = 10; // limite des articles
const LIMIT_WORD_SIDEBAR = 8; // limite de mots dans la sidebar